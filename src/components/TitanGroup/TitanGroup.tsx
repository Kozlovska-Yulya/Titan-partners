'use client';
import React from 'react';
import styles from './TitanGroup.module.scss';

const TitanGroup = () => {
  return (
    <section className={styles.titanGroup}>
      <div className={styles.titanGroup__track}>
        <span>TITAN GROUP / TITAN GROUP / TITAN GROUP / TITAN GROUP</span>
      </div>
    </section>
  );
};

export default TitanGroup;
